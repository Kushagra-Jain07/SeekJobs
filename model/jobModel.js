class JobModel {
    constructor(id, ownerId, description, address, salary, phone) {
        this.id = id,
            this.ownerId = ownerId,
            this.description = description,
            this.address = address,
            this.salary = salary,
            this.phone = phone
    }
}

export default JobModel